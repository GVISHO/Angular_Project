import { BackgroundImageRotatorDirective } from './background-image-rotator.directive';

describe('BackgroundImageRotatorDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundImageRotatorDirective();
    expect(directive).toBeTruthy();
  });
});
